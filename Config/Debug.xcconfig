// Local overrides (gitignored): copy Local.xcconfig.example -> Local.xcconfig
//
// IMPORTANT: URLs must be double-quoted — bare http://... is truncated at // because
// // starts an xcconfig line comment.
RANK_API_BASE_URL = "http://98.84.78.7:8000"
#include? "Local.xcconfig"
